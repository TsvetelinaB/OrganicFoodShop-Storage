@model List<ProductListingViewModel>

@{
    ViewData["Title"] = "Home Page";
}

<div>
    <h1 class="display-5 ">Welcome to "PoZdravi"!</h1>
    <p class="lead"> Enjoy healthy and organic food!</p>
</div>


@*@{
        var firstProduct = Model.Any() ? Model[0] : null;
        var secondProduct = Model.Count > 1 ? Model[1] : null;
        var thirdProduct = Model.Count > 2 ? Model[2] : null;
    }*@

<div class="jumbotron col-md-10">
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">

        @*<ol class="carousel-indicators">
                <li data-target="#carouselExampleIndicators" data-slide-to="@firstProduct" class="active"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="@secondProduct"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="@thirdProduct"></li>
            </ol>*@

        <div class="carousel-inner align-content-lg-center">

            @for (int i = 0; i < Model.Count; i++)
            {
                var product = Model[i];
                <div class="carousel-item @(i==0 ? "active" : string.Empty)">
                    <img src="@product.ImageURL" class="d-block w-100" alt="@product.Name">
                    <div class="carousel-caption d-none d-md-block">
                        <h5><a class="btn btn-info" asp-controller="Products" asp-action="Details" asp-route-id="@product.Id">See more</a></h5>
                    </div>
                </div>
            }


            @*@if (firstProduct != null)
                {
                    <div class="carousel-item active">
                        <img src="@firstProduct.ImageURL" class="d-block w-100" alt="@firstProduct.Name">
                        <div class="carousel-caption d-none d-md-block">
                            <h5><a class="btn btn-info" asp-controller="Products" asp-action="Details" asp-route-id="@firstProduct.Id">See more</a></h5>
                        </div>
                    </div>
                }

                @if (secondProduct != null)
                {
                    <div class="carousel-item">
                        <img src="@secondProduct.ImageURL" class="d-block w-100" alt="@secondProduct.Name">
                        <div class="carousel-caption d-none d-md-block">
                            <h5><a class="btn btn-info" asp-controller="Products" asp-action="Details" asp-route-id="@secondProduct.Id">See more</a></h5>
                        </div>
                    </div>
                }

                @if (thirdProduct != null)
                {
                    <div class="carousel-item">
                        <img src="@thirdProduct.ImageURL" class="d-block w-100" alt="@thirdProduct.Name">
                        <div class="carousel-caption d-none d-md-block">
                            <h5><a class="btn btn-info" asp-controller="Products" asp-action="Details" asp-route-id="@thirdProduct.Id">See more</a></h5>
                        </div>
                    </div>
                }*@
        </div>
        <a class="carousel-control-prev" role="button" href="#carouselExampleControls" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" role="button" href="#carouselExampleControls" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </a>
    </div>
</div>

@*<div class="row">
        <div class="col-md-4">
            <h2>Discover New </h2>
            <p>
                bla bla
            </p>
            <h5><a class="btn btn-info" asp-controller="Products" asp-action="AllProducts" asp-route-id="@firstProduct.Id">See All</a></h5>
        </div>
    </div>*@

@*@section Scripts{
    <script>
        $.get('api/statistics', (data) => {
            console.log(data);
        })
    </script>*@
}